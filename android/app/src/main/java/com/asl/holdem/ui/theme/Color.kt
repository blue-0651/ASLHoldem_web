package com.asl.holdem.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * ASL Holdem 브랜드 컬러 팔레트
 * Material Design 3 컬러 시스템을 기반으로 구성
 */

// Primary Colors (브랜드 메인 컬러 - 포커 그린 계열)
val ASLPrimary = Color(0xFF1B5E20)           // 진한 녹색
val ASLOnPrimary = Color(0xFFFFFFFF)         // 흰색
val ASLPrimaryContainer = Color(0xFF66BB6A)   // 연한 녹색
val ASLOnPrimaryContainer = Color(0xFF000000) // 검은색

// Primary Colors Dark
val ASLPrimaryDark = Color(0xFF66BB6A)        // 연한 녹색
val ASLOnPrimaryDark = Color(0xFF000000)      // 검은색
val ASLPrimaryContainerDark = Color(0xFF1B5E20) // 진한 녹색
val ASLOnPrimaryContainerDark = Color(0xFFFFFFFF) // 흰색

// Secondary Colors (보조 컬러 - 골드 계열)
val ASLSecondary = Color(0xFFFF8F00)          // 주황색/골드
val ASLOnSecondary = Color(0xFF000000)        // 검은색
val ASLSecondaryContainer = Color(0xFFFFE0B2) // 연한 주황색
val ASLOnSecondaryContainer = Color(0xFF000000) // 검은색

// Secondary Colors Dark
val ASLSecondaryDark = Color(0xFFFFB74D)      // 연한 주황색
val ASLOnSecondaryDark = Color(0xFF000000)    // 검은색
val ASLSecondaryContainerDark = Color(0xFFFF8F00) // 주황색/골드
val ASLOnSecondaryContainerDark = Color(0xFF000000) // 검은색

// Tertiary Colors (3차 컬러 - 레드 계열)
val ASLTertiary = Color(0xFFD32F2F)           // 레드
val ASLOnTertiary = Color(0xFFFFFFFF)         // 흰색
val ASLTertiaryContainer = Color(0xFFFFCDD2)  // 연한 레드
val ASLOnTertiaryContainer = Color(0xFF000000) // 검은색

// Tertiary Colors Dark
val ASLTertiaryDark = Color(0xFFEF5350)       // 연한 레드
val ASLOnTertiaryDark = Color(0xFF000000)     // 검은색
val ASLTertiaryContainerDark = Color(0xFFD32F2F) // 레드
val ASLOnTertiaryContainerDark = Color(0xFFFFFFFF) // 흰색

// Error Colors
val ASLError = Color(0xFFB00020)              // 에러 컬러
val ASLOnError = Color(0xFFFFFFFF)            // 흰색
val ASLErrorContainer = Color(0xFFFFDAD6)     // 연한 에러 컬러
val ASLOnErrorContainer = Color(0xFF000000)   // 검은색

// Error Colors Dark
val ASLErrorDark = Color(0xFFCF6679)          // 연한 에러 컬러
val ASLOnErrorDark = Color(0xFF000000)        // 검은색
val ASLErrorContainerDark = Color(0xFFB00020) // 에러 컬러
val ASLOnErrorContainerDark = Color(0xFFFFFFFF) // 흰색

// Surface Colors
val ASLBackground = Color(0xFFFFFBFE)         // 배경색
val ASLOnBackground = Color(0xFF1C1B1F)       // 배경색 위 텍스트
val ASLSurface = Color(0xFFFFFBFE)            // 서페이스 색
val ASLOnSurface = Color(0xFF1C1B1F)          // 서페이스 위 텍스트
val ASLSurfaceVariant = Color(0xFFE7E0EC)     // 서페이스 변형
val ASLOnSurfaceVariant = Color(0xFF49454F)   // 서페이스 변형 위 텍스트

// Surface Colors Dark
val ASLBackgroundDark = Color(0xFF1C1B1F)     // 다크 배경색
val ASLOnBackgroundDark = Color(0xFFE6E1E5)   // 다크 배경색 위 텍스트
val ASLSurfaceDark = Color(0xFF1C1B1F)        // 다크 서페이스 색
val ASLOnSurfaceDark = Color(0xFFE6E1E5)      // 다크 서페이스 위 텍스트
val ASLSurfaceVariantDark = Color(0xFF49454F) // 다크 서페이스 변형
val ASLOnSurfaceVariantDark = Color(0xFFCAC4D0) // 다크 서페이스 변형 위 텍스트

// Outline Colors
val ASLOutline = Color(0xFF79747E)            // 외곽선
val ASLOutlineVariant = Color(0xFFCAC4D0)     // 외곽선 변형

// Outline Colors Dark
val ASLOutlineDark = Color(0xFF938F99)        // 다크 외곽선
val ASLOutlineVariantDark = Color(0xFF49454F) // 다크 외곽선 변형

// Inverse Colors
val ASLInverseSurface = Color(0xFF313033)     // 역 서페이스
val ASLInverseOnSurface = Color(0xFFF4EFF4)   // 역 서페이스 위 텍스트
val ASLInversePrimary = Color(0xFF66BB6A)     // 역 프라이머리

// Inverse Colors Dark
val ASLInverseSurfaceDark = Color(0xFFE6E1E5) // 다크 역 서페이스
val ASLInverseOnSurfaceDark = Color(0xFF313033) // 다크 역 서페이스 위 텍스트
val ASLInversePrimaryDark = Color(0xFF1B5E20) // 다크 역 프라이머리

// Surface Tint & Scrim
val ASLSurfaceTint = Color(0xFF1B5E20)        // 서페이스 틴트
val ASLScrim = Color(0xFF000000)              // 스크림

// Surface Tint & Scrim Dark
val ASLSurfaceTintDark = Color(0xFF66BB6A)    // 다크 서페이스 틴트
val ASLScrimDark = Color(0xFF000000)          // 다크 스크림

// Additional Custom Colors (앱 특화 컬러)
val ASLPokerGreen = Color(0xFF1B5E20)         // 포커 테이블 녹색
val ASLChipGold = Color(0xFFFF8F00)           // 칩 골드색
val ASLCardRed = Color(0xFFD32F2F)            // 카드 레드색
val ASLCardBlack = Color(0xFF000000)          // 카드 블랙색

// Gradient Colors (그라데이션용)
val ASLGradientStart = Color(0xFF1B5E20)      // 그라데이션 시작
val ASLGradientEnd = Color(0xFF66BB6A)        // 그라데이션 끝 